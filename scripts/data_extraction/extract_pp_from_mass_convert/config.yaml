# Job controls
job:
  name: "extract_pp"
  l_batch: True
  l_extract: True
  l_copy_extracted: False
  l_convert: True
  l_copy_converted: False

# SLURM settings
slurm:
  account: "acsis"
  partition: "standard"
  qos: "short"
  time: "10:00"
  memory: "2000"

jobs:
  array_range: "1-143"

# Paths
path:
  tmp_dir: "/work/scratch-pw5/vs480"
  extracted_save_dir: "/gws/nopw/j04/acsis/vs480/model_output" # optional. Use only if l_copy_downloaded=True or l_use_downloaded_save_dir=True
  converted_save_dir: "/gws/nopw/j04/acsis/vs480/model_output" # optional. Use only if l_copy_converted=True
  processing_queue: "/home/users/vs480/git/group_python/informal/vs480/processing_queues/processing_queue_DEST-flux-diags.csv" # absolute path or relative to working directory

# MASS extracting options
extract:
  l_extra_query: False
  max_retries: 2 # optional
  walltime: "2:00:00"
  # query_options: "./query_options.txt" # optional. Use only if l_extra_query=True

# Processing options
convert:
  l_use_extracted_save_dir: False # optional. For converting from pp files in downloaded_save_dir
  format: "nc" # 'nc' or 'zarr'. only 'nc' is available now
