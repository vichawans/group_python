# Job controls
job:
  name: "UKESM_data_download"
  l_batch: True
  l_download: True
  l_copy_downloaded: True
  l_convert: False
  l_copy_converted: False

# SLURM settings
slurm:
  account: "acsis"
  partition: "debug"
  qos: "debug"
  time: "1:00:00"
  memory: "200000"
  array_range: "1-2"

# Paths
path:
  tmp_dir: "/work/scratch-pw2/vs480"
  downloaded_save_dir: "/gws/nopw/j04/acsis/vs480/model_output" # optional
  converted_save_dir: "/gws/nopw/j04/acsis/vs480/model_output" # optional
  processing_queue: "./processing_queue_u-bp901.csv"

# Retrieving options
download:
  l_extra_query: False
  max_retries: 3 # optional
  # query_options: "./query_options.txt"

# Processing options
convert:
  l_use_downloaded_save_dir: False # optional. For converting from pp files in downloaded_save_dir
  conda_env: "cmip6-env"
  format: "nc" # 'nc' or 'zarr'
